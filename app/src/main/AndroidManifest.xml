<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufs.cienciainformacao.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.TelaInicial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.TelaPesquisa" />
        <activity android:name=".activities.TelaLeitura" />
        <activity android:name=".activities.TelaGaleriaLivros" />
        <activity android:name=".activities.TelaHistoricoBusca" />
        <activity android:name=".activities.TelaDownload" />
        <activity android:name=".activities.TelaAreasInteresse" />
        <activity android:name=".activities.TelaOpcoesDeBusca" />
        <activity android:name=".activities.TelaDownloadCategoria"/>

    </application>

</manifest>